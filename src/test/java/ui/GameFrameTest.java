package ui;

import java.awt.*;

public class GameFrameTest {
    GameFrame gameFrame;
    Panel buttonsPanel;
    Component[] components;
    Button buttonOne;
    Button buttonTwo;
    Button buttonThree;
    /*
    @Before
    public void setUp() throws Exception {

        gameFrame = Mockito.mock(GameFrame.class, Mockito.CALLS_REAL_METHODS);
        buttonsPanel = gameFrame.createButtonsPanel();
        components = buttonsPanel.getComponents();

        buttonOne = (Button) components[0];
        buttonTwo = (Button) components[1];
        buttonThree = (Button) components[2];
    }

    @Test
    public void button_panel_should_have_three_buttons() {
        if (!GraphicsEnvironment.isHeadless()) {
            Frame frame = new Frame("test");
            frame.setVisible(true);
        }
        int actual = buttonsPanel.getComponentCount();
        assertEquals(actual, 3);
    }

    @Test
    public void button_one_is_choice_one() {
        assertEquals("Choice One", buttonOne.getLabel());
    }

    @Test
    public void button_one_has_correct_set_bounds() {
        Rectangle actual = buttonOne.getBounds();
        assertEquals(new Rectangle(60, 60, 100, 100), actual);
    }

    @Test
    public void button_two_is_created_with_label_choice_one() {
        assertEquals("Choice Two", buttonTwo.getLabel());
    }

    @Test
    public void button_two_has_correct_set_bounds() {
        Rectangle actual = buttonTwo.getBounds();
        assertEquals(new Rectangle(60, 60, 100, 100), actual);
    }

    @Test
    public void button_three_is_created_with_label_choice_one() {
        assertEquals("Choice Three", buttonThree.getLabel());
    }

    @Test
    public void button_three_has_correct_set_bounds() {
        Rectangle actual = buttonThree.getBounds();
        assertEquals(new Rectangle(60, 60, 100, 100), actual);
    }
     */


}
